import { NhostClient } from '@nhost/react'

const nhost = new NhostClient({
  subdomain: 'dfpyikmcxorwrzvyxevh',
  region: 'us-east-1',
  clientStorageType: 'expo-secure-storage',
  // clientStorage: SecureStore,
});